name: hello-world-nandhakumarbm
on: push
jobs:
  docker-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Your Register Number
        run: echo "Hello-World - 22IT053"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Build
        run: docker build -t vettri12/ml-model-22IT053 .

      - name: Docker Images
        run: docker images
